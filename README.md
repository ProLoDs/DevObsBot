# RocketChatBot
In near future this should be an event-based Bot in Python3.

## WARNING WIP
Warning: This project is in it's early stages and probably won't do what it is supposed to.
Feel free to contribute.

## Installation

` pip install -r requirements.txt`

## Usage

Register a handler with a prefix in the box and add behaviour.
For examples look at main.py